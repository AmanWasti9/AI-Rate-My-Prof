{
  "version": "1",
  "phases": {
    "setup": {
      "commands": [
        "cd frontend",
        "npm install"
      ]
    },
    "build": {
      "commands": [
        "cd frontend",
        "npm run build"
      ]
    },
    "start": {
      "commands": [
        "cd frontend",
        "npx serve -s build"
      ]
    }
  },
  "env": {
    "PORT": "3000"
  },
  "language": {
    "name": "node",
    "version": "18"
  }
}
